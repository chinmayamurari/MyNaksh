version: v1.0
contract_type: baseline
description: Baseline ICE contract for MyNakshChat — allows work in canonical risk domains plus the custom chat UI domain; file deletion is disallowed unless explicitly enabled.
allowed_domains:
  - governance
  - auth
  - identity
  - infra
  - db_schema
  - secrets
  - prod_config
  - chat_ui
allowed_actions:
  file_deletion: false


